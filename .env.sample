# CarrotQuant Data (.env.sample)
# 复制此文件为 .env 即可在本地自定义配置 (默认被 .gitignore 忽略)

# 数据持久化存储根目录 (默认为当前工作目录下的 data)
CQDATA_DATA_DIR=data

# 日志输出目录与级别
CQDATA_LOG_DIR=logs
CQDATA_LOG_LEVEL=INFO

# 可选：指定全局 YAML 配置文件路径
# CQDATA_CONFIG_PATH=./config/config.yaml
